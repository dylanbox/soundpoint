class Point {
    // A point in space

    public float x, y, z, intensity;

    Point(float temp_x, float temp_y, float temp_z, float temp_intensity) {
        x = temp_x;
        y = temp_y;
        z = temp_z;
        intensity = temp_intensity;
    }
}